import React,{useEffect,useState}from"react";import{Link}from"react-router-dom";import Navbar from"../components/Navbar";import Footer from"../components/Footer";import{API}from"../context/AuthContext";import"./Blogs.css";
export default function Blogs(){const[posts,setPosts]=useState([]),[q,setQ]=useState(""),[cat,setCat]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{fetch(`${API}/blogs?status=published`).then(r=>r.json()).then(d=>setPosts(d.blogs||[])).catch(()=>setPosts([])).finally(()=>setLoading(false))},[]);const cats=[...new Set(posts.map(p=>p.category).filter(Boolean))];const filtered=posts.filter(p=>(!cat||p.category===cat)&&(!q||`${p.title} ${p.excerpt} ${p.tags?.join(" ")}`.toLowerCase().includes(q.toLowerCase())));return <div className="blog-page"><Navbar/><section className="blog-hero"><div className="blog-container"><div className="blog-eyebrow">The GoNbite Journal</div><h1>Stories worth<br/><span>tasting.</span></h1><p>Food stories, local dining ideas, restaurant insights and practical guides from the GoNbite team.</p></div></section><main className="blog-container"><div className="blog-toolbar"><input className="blog-search" aria-label="Search blogs" placeholder="Search stories…" value={q} onChange={e=>setQ(e.target.value)}/><select className="blog-filter" value={cat} onChange={e=>setCat(e.target.value)}><option value="">All categories</option>{cats.map(c=><option key={c}>{c}</option>)}</select></div><div className="blog-grid">{loading?<div className="blog-empty">Loading stories…</div>:filtered.length?filtered.map(p=><article className="blog-card" key={p._id}><img className="blog-card-img" src={p.coverImage||"/gonbite-icon.png"} alt={p.coverImageAlt||p.title}/><div className="blog-card-body"><div className="blog-meta"><span>{p.category||"Food"}</span><span>{new Date(p.publishedAt||p.createdAt).toLocaleDateString()}</span></div><h2>{p.title}</h2><p>{p.excerpt}</p><Link className="blog-read" to={`/blogs/${p.slug}`}>Read story →</Link></div></article>):<div className="blog-empty">No stories match your search.</div>}</div></main><Footer/></div>}
