import React,{useState}from"react";import{Link,useNavigate}from"react-router-dom";import AccountShell from"../components/AccountShell";import{API}from"../context/AuthContext";
export default function AdminRegister(){const[f,setF]=useState({name:"",email:"",password:"",adminKey:""}),[err,setErr]=useState(""),[ok,setOk]=useState(""),[load,setLoad]=useState(false),nav=useNavigate();const submit=async e=>{e.preventDefault();setErr("");setOk("");setLoad(true);try{const r=await fetch(`${API}/auth/admin/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)}),d=await r.json();if(!r.ok)throw new Error(d.message);setOk(d.message);setTimeout(()=>nav("/login"),1300)}catch(e){setErr(e.message)}finally{setLoad(false)}};return <AccountShell admin kicker="Protected setup" title="Register administrator"><p>This route requires the private administrator registration key configured on the server.</p><form className="form-grid" onSubmit={submit}>{err&&<div className="form-error">{err}</div>}{ok&&<div className="form-success">{ok}</div>}<div className="form-field"><label>Name</label><input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></div><div className="form-field"><label>Email</label><input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></div><div className="form-field"><label>Password</label><input type="password" minLength="12" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></div><div className="form-field"><label>Administrator registration key</label><input type="password" required value={f.adminKey} onChange={e=>setF({...f,adminKey:e.target.value})}/></div><button className="form-submit" disabled={load}>{load?"Creating…":"Create administrator"}</button><div className="form-link"><Link to="/login">Back to admin login</Link></div></form></AccountShell>}
